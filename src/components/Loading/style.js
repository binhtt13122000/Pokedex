import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    loadingImage: {
        display: 'block',
        margin: '0 auto',
        marginTop: 'auto',
        marginBottom: 'auto'
    },
    loadingBackground: {
        minHeight: '70vh'
    }
})